### Defect Class
```matlab
classdef defect < player
```
#### Attributes
None
#### Methods
- Constructor
    ```matlab
    function obj = defect(~)
    ```
    Creates a **defect** player.

- Move
    ```matlab
    function obj = setMove(obj, ~, ~, ~)
    ```
    Always defects.