### Random Class
```matlab
classdef random < player
```
#### Attributes
None
#### Methods
- Constructor
    ```matlab
    function obj = random(~)

    ```
    Creates a Random player.

- Move
    ```matlab
    function obj = setMove(obj, ~, ~, ~)
    ```
    Makes a random move.